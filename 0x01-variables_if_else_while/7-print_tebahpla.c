#include <stdio.h>
/**
 * main - Entry point
 * Description: Print lowercase alphabet in reverse. Using only putchar.
 * Return: Always 0 (Success)
 */
int main(void)
{
        char alphabets;

        for (alphabets = "z"; alphabets >=; --alphabets)
        {
                putchar(alphabets);
        }
	putchar('\n');

                return (0);
