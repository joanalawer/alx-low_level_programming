#include <stdio.h>
/**
 * main - Entry point
 * Description: Print single digit numbers of base 10, from 0
 * Return: Always 0 (Success)
 */
int main(void)
{
        int putchar(int i);

        for (i = 0; i < 10; ++i)
        {
                putchar(i);
        }
                return (0);
}
