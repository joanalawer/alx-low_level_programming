#include "main.h"
/**
 * add - Adds two integers and returns the result
 * @a: First Integer to be added
 * @b: Second integer to be added
 * Return: Result (sum)
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

return (sum);
}
