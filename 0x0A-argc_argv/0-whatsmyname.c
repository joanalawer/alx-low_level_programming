#include <stdio.h>
/**
 * main - Entry point
 * @argc: prints number of arguments
 * @argv: prints char of arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argv[1]);
	return (0);
}
