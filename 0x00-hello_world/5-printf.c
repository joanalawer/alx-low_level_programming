#include <stdio.h>
/**
 * main - Entry point
 * Description: Program uses printf() to print a line of text
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
