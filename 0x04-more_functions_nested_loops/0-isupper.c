#include "main.h"
/**
 * _isupper - Function checks for uppercase character
 * Return: 1 if c ic uppercase, 0 otherwise
 */
int _isupper(int c);
{
	char ch = 'A';

	if (_isupper(ch))
		return(1);
	else
		return (0);
}
